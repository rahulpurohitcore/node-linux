[Unit]
Description={{description}}

[Service]
WorkingDirectory={{path}}
ExecStart={{execpath}}
Restart=always
SyslogIdentifier={{label}}
User={{user}}
Group={{group}}
Environment={{env}}

[Install]
WantedBy=multi-user.target
